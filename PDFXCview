#!/bin/bash

# Выводим информацию, чтобы она попала в stdout (и потом в логи бота)
echo "=== MOCK PDFXCview ==="
echo "Симуляция печати запущена..."
echo "Получены аргументы: $@"
echo "Печать 'успешно' завершена."

# Возвращаем код 0 (успех), чтобы subprocess в Python не выдал ошибку
exit 0
